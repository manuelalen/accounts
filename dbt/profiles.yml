supabase_profile:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "db.fzdpeakdudjfyvgrhrqq.supabase.co"
      user: "postgres"
      password: "{{ env_var('DBT_ENV_SECRET_PASSWORD') }}"
      port: 5432
      dbname: "postgres"
      schema: bronze
      threads: 1